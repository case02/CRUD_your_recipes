<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <%- include ('./partials/head.ejs') %>
</head>

<body>
    <div class="body-overlay">
        <!-- ============ HEADER ============ -->
        <%- include ('./partials/header.ejs') %>
        <!-- ============ MAIN ============ -->
        <main class="show-main">
            <h1><%= recipe.name %></h1>
            <div id="show-item">
                <img src="<%= recipe.image %>" width="500" />
                <p><%= recipe.description %></p>
                <p>Prep time in min: <%= recipe.prepTime %></p>
                <p>Cook time in min: <%= recipe.cookTime %>
                </p>
                <p>Servings: <%= recipe.servings %>
                </p>
                <p>Ingredients: <%= recipe.ingredients %>
                </p>
                <p>Directions: <%= recipe.directions %>
                </p>
                <a href='/recipe/<%= recipe._id %>/edit'><button>EDIT</button></a>
                <form class="delete" action="/recipe/<%= recipe._id %>?_method=DELETE" method="POST">
                    <button class="delete" type="submit">DELETE</button>
                </form>
            </div>
        </main>
    </div>
</body>

</html>